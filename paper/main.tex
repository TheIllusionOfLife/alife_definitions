\documentclass[letterpaper]{article}
\usepackage{natbib,alifeconf}
\setcounter{secnumdepth}{2}  % enable section numbering (alifeconf sets 0 by default)
\usepackage{graphicx,amsmath,amssymb,booktabs,xcolor,multirow}
\usepackage{url,hyperref}
\usepackage{ifthen}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta}
% Blind footnote (no marker) — from ALIFE 2026 template
\makeatletter
\def\blfootnote{\xdef\@thefnmark{}\@footnotetext}
\makeatother
\newcommand{\includefigure}[2][width=\columnwidth]{%
  \IfFileExists{#2}{\includegraphics[#1]{#2}}{%
    \fbox{\parbox{0.8\columnwidth}{\centering\vspace{1em}%
      \textit{Figure placeholder}\vspace{1em}}}}}

% --- Result values (from analysis pipeline; calibration: seeds 0--99, held-out test: seeds 100--199, 5 regimes) ---
\newcommand{\kappaDD}[1]{%
  \ifthenelse{\equal{#1}{D1D2}}{0.89}{%
  \ifthenelse{\equal{#1}{D1D3}}{0.44}{%
  \ifthenelse{\equal{#1}{D1D4}}{0.29}{%
  \ifthenelse{\equal{#1}{D2D3}}{0.39}{%
  \ifthenelse{\equal{#1}{D2D4}}{0.24}{%
  \ifthenelse{\equal{#1}{D3D4}}{0.17}{??}}}}}}}
\newcommand{\rhoDD}[1]{%
  \ifthenelse{\equal{#1}{D1D2}}{0.67}{%
  \ifthenelse{\equal{#1}{D1D3}}{0.78}{%
  \ifthenelse{\equal{#1}{D1D4}}{0.74}{%
  \ifthenelse{\equal{#1}{D2D3}}{0.58}{%
  \ifthenelse{\equal{#1}{D2D4}}{0.39}{%
  \ifthenelse{\equal{#1}{D3D4}}{0.61}{??}}}}}}}
\newcommand{\fleissK}{0.42}
\newcommand{\aucD}[1]{%
  \ifthenelse{\equal{#1}{D1}}{0.85}{%
  \ifthenelse{\equal{#1}{D2}}{0.65}{%
  \ifthenelse{\equal{#1}{D3}}{0.76}{%
  \ifthenelse{\equal{#1}{D4}}{0.78}{??}}}}}
% --- 95% bootstrap CIs (2000 block-bootstrap resamples over seeds) ---
\newcommand{\kappaDDci}[1]{%
  \ifthenelse{\equal{#1}{D1D2}}{[0.87,\,0.91]}{%
  \ifthenelse{\equal{#1}{D1D3}}{[0.39,\,0.48]}{%
  \ifthenelse{\equal{#1}{D1D4}}{[0.25,\,0.33]}{%
  \ifthenelse{\equal{#1}{D2D3}}{[0.35,\,0.42]}{%
  \ifthenelse{\equal{#1}{D2D4}}{[0.21,\,0.28]}{%
  \ifthenelse{\equal{#1}{D3D4}}{[0.12,\,0.22]}{??}}}}}}}
\newcommand{\rhoDDci}[1]{%
  \ifthenelse{\equal{#1}{D1D2}}{[0.66,\,0.69]}{%
  \ifthenelse{\equal{#1}{D1D3}}{[0.76,\,0.79]}{%
  \ifthenelse{\equal{#1}{D1D4}}{[0.72,\,0.76]}{%
  \ifthenelse{\equal{#1}{D2D3}}{[0.56,\,0.61]}{%
  \ifthenelse{\equal{#1}{D2D4}}{[0.36,\,0.42]}{%
  \ifthenelse{\equal{#1}{D3D4}}{[0.58,\,0.63]}{??}}}}}}}
\newcommand{\fleissKci}{[0.39,\,0.44]}
\newcommand{\aucDci}[1]{%
  \ifthenelse{\equal{#1}{D1}}{[0.82,\,0.87]}{%
  \ifthenelse{\equal{#1}{D2}}{[0.62,\,0.67]}{%
  \ifthenelse{\equal{#1}{D3}}{[0.73,\,0.78]}{%
  \ifthenelse{\equal{#1}{D4}}{[0.75,\,0.81]}{??}}}}}
\newcommand{\aucDstrict}[1]{%
  \ifthenelse{\equal{#1}{D1}}{0.85}{%
  \ifthenelse{\equal{#1}{D2}}{0.65}{%
  \ifthenelse{\equal{#1}{D3}}{0.76}{%
  \ifthenelse{\equal{#1}{D4}}{0.54}{??}}}}}
\newcommand{\aucDciStrict}[1]{%
  \ifthenelse{\equal{#1}{D1}}{[0.82,\,0.87]}{%
  \ifthenelse{\equal{#1}{D2}}{[0.62,\,0.67]}{%
  \ifthenelse{\equal{#1}{D3}}{[0.73,\,0.78]}{%
  \ifthenelse{\equal{#1}{D4}}{[0.50,\,0.57]}{??}}}}}
% --- End result values ---

\title{Life Definitions Disagree: An Empirical Benchmark\\of Competing Operationalizations in a Shared Digital Ecology}
\author{
    Yuya Mukai$^{1}$ \\
    \mbox{}\\
    $^1$Mukai Entertainment, Japan \\
    mukaiyuya@gmail.com
}

\begin{document}
\maketitle

% ============================================================================
\begin{abstract}
Multiple operational definitions of life exist, yet they are rarely applied to
the same system under controlled conditions.  We present an empirical benchmark
comparing four definitions---textbook 7-criteria (D1), Darwinian/NASA (D2),
autonomy/organizational closure (D3), and information maintenance (D4)---by
scoring the same organism populations across five environment regimes in a
shared digital ecology.  Three co-existing family types with different ablated
capabilities serve as natural controls.  We report the disagreement structure
among definitions, identify regime-dependent divergences, and evaluate
predictive validity against population robustness within our substrate and
prediction targets.  Evaluation mode substantially affects predictive ranking:
under strict (leakage-aware) evaluation, D4's AUC drops from 0.78 to 0.54
while D1 remains at 0.85, revealing that D4's causal component currently
captures population-level rather than organism-level information maintenance.
Our benchmark suite and adapter code are released as an open artifact for the
ALife community.
\end{abstract}

Submission type: \textbf{Full Paper}

Data/Code available at: \url{https://github.com/TheIllusionOfLife/alife_definitions}

\blfootnote{\textcopyright 2026 Yuya Mukai. Published under a Creative Commons Attribution 4.0 International (CC~BY~4.0) license.}

% ============================================================================
\section{Introduction}
\label{sec:intro}

What counts as ``alive'' depends on whom you ask.  Textbook biology
lists seven properties (metabolism, growth, reproduction, etc.); NASA's
working definition foregrounds Darwinian evolution; autopoiesis theory
emphasizes organizational closure; and information-theoretic accounts
stress causal self-maintenance of heritable information
\citep{campbell_2017_biology, joyce_1994_foreword, varela_1974_autopoiesis,
schrodinger_1944_what}.  Each definition captures a genuine aspect of living
systems, yet they are rarely applied to the same system under controlled
conditions.  A researcher who adopts one definition may classify a digital
organism as ``alive'' while another, using a different definition, rejects it.

This disagreement is not merely philosophical.  In astrobiology, the choice
of life definition determines which biosignatures to search for
\citep{benner_2010_defining}.  In artificial life, it shapes which systems
we consider successful.  Yet no prior work has systematically compared
multiple operational definitions on the same digital organisms in the same
environment.

We address this gap by constructing an \emph{empirical benchmark}.  We
implement four definitions as scoring adapters that consume identical
organism trace data from a shared digital ecology.  Three family types---one
with all capabilities enabled (F1), one lacking boundary and homeostasis
(F2, ``Darwinian''), and one lacking reproduction and evolution
(F3, ``autonomy'')---coexist in five environment regimes, providing natural
controls where definitions are expected to disagree.  We report:
\begin{enumerate}
\item The pairwise agreement structure (Cohen's $\kappa$, Spearman $\rho$)
  among D1--D4.
\item Regime-dependent divergences where environmental stress alters
  which definitions accept or reject a family.
\item Predictive validity: which definition's graded score best predicts
  robust population persistence under unseen conditions (held-out seeds
  100--199 within the same five regimes), under both legacy and strict
  (leakage-aware) evaluation modes.
\end{enumerate}

All code, adapters, and benchmark data are released as an open artifact.

% ============================================================================
\section{Related Work}
\label{sec:related}

\paragraph{Definitions of life.}
\citet{campbell_2017_biology} codifies the textbook 7-criteria list.
The NASA/Darwinian definition, attributed to \citet{joyce_1994_foreword},
frames life as ``a self-sustaining chemical system capable of Darwinian
evolution.''  \citet{varela_1974_autopoiesis} introduced autopoiesis---the
idea that living systems are defined by organizational closure,
where constituent processes mutually produce the network that produces them.
\citet{schrodinger_1944_what} anticipated the information-theoretic view,
emphasizing that organisms maintain internal order against entropy;
\citet{walker_2013_algorithmic} extend this to an algorithmic framework
where life is distinguished by top-down informational causation.

\citet{bedau_2008_nature} surveys the landscape of life definitions and
argues that no single criterion is sufficient.
\citet{ruizmirazo_2004_universal} propose a synthesis of \emph{basic
autonomy} (metabolic self-maintenance) and \emph{open-ended evolution},
bridging the D2--D3 tension that our benchmark foregrounds.
\citet{cleland_2002_defining} and \citet{benner_2010_defining} further
argue that each definition captures a necessary but insufficient
aspect.  Our benchmark operationalizes this pluralist insight: rather than
choosing one definition, we instrument all four and compare.

\paragraph{ALife benchmarking.}
Digital evolution platforms such as Avida \citep{ofria_2004_avida} and
cellular-automaton worlds like Lenia \citep{chan_2019_lenia} and
Flow-Lenia \citep{plantec_2023_flow} have been used to study individual
properties of life (e.g., open-ended evolution, self-organization).
\citet{dolson_2019_modes} introduced the MODES toolbox for measuring
open-ended dynamics, providing statistical diagnostics for evolutionary
systems; however, these efforts typically adopt a single operational
criterion and do not cross-compare definitions on the same organisms.
Our contribution is the \emph{benchmarking methodology}: a shared substrate,
controlled family ablations, and a pluggable adapter API that allows new
definitions to be added without modifying the simulator.  Our benchmark
instruments all four definitions on shared organisms, reporting disagreements
as empirical data rather than collapsing ontological differences: each adapter
implements one definition's measurement regime independently.

% ============================================================================
\section{System and Baseline}
\label{sec:system}

\paragraph{Hybrid substrate.}
Our digital ecology uses a two-layer agent-based model implemented in Rust
with Python bindings (PyO3/maturin).  The lower layer consists of
\emph{swarm agents} (10--50 per organism) that execute a small neural
network controlling movement, energy harvesting, waste excretion, and
boundary maintenance.  The upper layer aggregates agents into
\emph{organisms}: coherent spatial clusters that can grow, reproduce
(by fission), and die.  Organisms inhabit a continuous 2D toroidal
world with diffusing resource and waste fields.
The world $\to$ organism $\to$ agent hierarchy means that each organism's
behavior emerges from collective agent dynamics, while population-level
patterns emerge from organism interactions (Figure~\ref{fig:architecture}).

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    box/.style={draw, rounded corners, minimum width=2.4cm, minimum height=0.7cm,
                align=center, font=\small},
    arr/.style={->, thick},
]
% World level
\node[box, fill=blue!12] (world) {World\\[-1pt]{\footnotesize 2D toroidal, resources}};

% Organism level
\node[box, fill=green!12, below left=1.0cm and 0.3cm of world] (org1)
    {Organism\\[-1pt]{\footnotesize genome, metabolism}};
\node[box, fill=green!12, below right=1.0cm and 0.3cm of world] (org2)
    {Organism\\[-1pt]{\footnotesize genome, metabolism}};

% Agent level
\node[box, fill=orange!12, below=0.8cm of org1, minimum width=1.4cm, font=\scriptsize] (a1)
    {Agents\\10--50};
\node[box, fill=orange!12, below=0.8cm of org2, minimum width=1.4cm, font=\scriptsize] (a2)
    {Agents\\10--50};

% Arrows
\draw[arr] (world) -- node[left, font=\scriptsize, pos=0.4] {fields} (org1);
\draw[arr] (world) -- node[right, font=\scriptsize, pos=0.4] {fields} (org2);
\draw[arr] (org1) -- node[left, font=\scriptsize] {NN} (a1);
\draw[arr] (org2) -- node[right, font=\scriptsize] {NN} (a2);

% Feedback arrows
\draw[arr, gray, dashed] (a1.west) -- +(-0.4,0) |- node[left, font=\scriptsize, pos=0.25]
    {emerge} (org1.west);
\draw[arr, gray, dashed] (a2.east) -- +(0.4,0) |- node[right, font=\scriptsize, pos=0.25]
    {emerge} (org2.east);

% Level labels
\node[right=0.1cm of world, font=\scriptsize\itshape, gray] {population};
\node[left=0.1cm of org1, font=\scriptsize\itshape, gray] {individual};
\node[left=0.1cm of a1, font=\scriptsize\itshape, gray] {swarm};

% Reproduction arrow between organisms
\draw[arr, densely dotted] (org1) -- node[above, font=\scriptsize] {fission} (org2);
\end{tikzpicture}%
}%
\caption{Three-level substrate architecture.  The world provides
resource and waste fields to organisms; each organism's behavior
emerges from 10--50 swarm agents executing a genome-encoded neural
network.  Reproduction occurs by fission with heritable mutation.}
\label{fig:architecture}
\end{figure}

Each organism carries a heritable genome (256 floats) encoding neural-network
weights.  Reproduction introduces point mutations, enabling Darwinian
dynamics.  A configurable set of Boolean flags controls which capabilities
are active per family (metabolism, boundary maintenance, homeostasis,
stimulus response, reproduction, evolution, growth).

\paragraph{Simulation mode.}
The simulator's \emph{multi-family mode} configures multiple co-existing
family types, each with distinct capability flags, sharing the same world.
The co-existing family ablations serve as natural controls where definitions
are expected to disagree, enabling within-run comparisons.

\paragraph{Mode~B configuration.}
All benchmark runs place three family types in the same
world, competing for the same resources.  Each family starts with 10
organisms (30 total), each with 25 agents.  Runs proceed for 2{,}000
time steps with samples every 10 steps (200 sample points), providing
sufficient time-series length for transfer entropy and Granger causality
estimation.

To disentangle competition effects from capability deficits, we additionally
run \emph{single-family controls}: Mode~B with a single family entry,
preserving the full adapter pipeline while eliminating inter-family
resource competition (see \S\ref{sec:experiments}).

\paragraph{Family profiles.}
\textbf{F1} (full) has all seven capabilities enabled and serves as the
positive control.  \textbf{F2} (Darwinian) disables boundary maintenance
and homeostasis, retaining reproduction and evolution---it should satisfy
D2 but score lower on D3.  \textbf{F3} (autonomy) disables reproduction
and evolution, retaining metabolism, boundary maintenance, homeostasis,
and growth---it should satisfy D3 but fail D2.

% ============================================================================
\section{Definition Adapters}
\label{sec:adapters}

Each adapter consumes a run summary (JSON) and a family ID, and returns a
graded score $S \in [0, 1]$ plus a binary pass/fail label at a configurable
threshold (default 0.3).  All adapters share a common \texttt{AdapterResult}
dataclass.

\subsection{D1: Textbook 7-Criteria}

D1 operationalizes the textbook list
\citep{campbell_2017_biology} by assessing each of seven criteria
through three conditions.  Table~\ref{tab:d1_mapping} maps each criterion
to its simulator variable and downstream coupling target.

\begin{table}[t]
\centering
\small
\caption{D1 criterion-to-variable mapping.}
\label{tab:d1_mapping}
\begin{tabular}{@{}lll@{}}
\toprule
Criterion & Primary signal & Coupling target \\
\midrule
Metabolism     & \texttt{energy\_mean}   & \texttt{boundary\_mean} \\
Organization   & \texttt{boundary\_mean} & \texttt{energy\_mean} \\
Homeostasis    & \texttt{energy\_mean}   & \texttt{waste\_mean} \\
Growth         & \texttt{maturity\_mean} & \texttt{alive\_count} \\
Reproduction   & \texttt{birth\_count}   & \texttt{alive\_count} \\
Stimuli resp.  & \texttt{maturity\_mean} & \texttt{energy\_mean} \\
Evolution      & \texttt{genome\_diversity} & \texttt{alive\_count} \\
\bottomrule
\end{tabular}
\end{table}

The coupling targets reflect the simulator's feedback architecture: energy
consumption directly maintains boundary integrity (Metabolism$\to$\texttt{boundary\_mean}),
energy regulation governs waste handling (Homeostasis$\to$\texttt{waste\_mean}),
and maturity captures sensory-motor development relevant to stimulus response
(Stimuli resp.$\to$\texttt{energy\_mean}).  Multiple reasonable mappings exist;
sensitivity to coupling choice is assessed in \S\ref{sec:results}.

The three conditions are:
\begin{itemize}
\item $\alpha$ --- \emph{Dynamic process}: the coefficient of variation (CV)
  of the criterion's primary signal exceeds a noise floor, confirming
  active rather than static behavior.
\item $\beta$ --- \emph{Measurable degradation}: Cohen's $d$ effect size
  comparing the target family's signal against a family that \emph{lacks}
  the criterion (cross-family ablation test).
\item $\gamma$ --- \emph{Feedback coupling}: the maximum of transfer entropy
  \citep{schreiber_2000_measuring} and lagged cross-correlation (max lag~5)
  from the criterion's signal to its downstream target.
  The cross-correlation complement provides robustness when transfer entropy
  estimates are noisy at moderate sample sizes.
\end{itemize}

Each criterion score is $w_\alpha \cdot \alpha + w_\beta \cdot
\sigma(\beta) + w_\gamma \cdot \gamma$ with default weights
$(w_\alpha, w_\beta, w_\gamma) = (0.3, 0.4, 0.3)$, where
$\sigma(\beta) = 1/(1 + e^{-\beta})$ maps Cohen's $d$ to $[0,1]$
via the standard logistic function (scale $k=1$).
The aggregate is the geometric mean of all seven criteria.
The geometric mean ensures that \emph{any} criterion
scoring zero pulls the aggregate to zero, making D1 the strictest
definition.  We verify robustness to weight choice via $\pm 20\%$
perturbation analysis (\S\ref{sec:results}).

\subsection{D2: Darwinian / NASA}

D2 operationalizes ``a self-sustaining chemical system capable of Darwinian
evolution'' \citep{joyce_1994_foreword} through three sub-scores:

\begin{itemize}
\item $S_\text{reprod}$ --- Sustained reproduction: persistence of per-step
  births in the last 50\% of the run, combined with lineage event count.
\item $S_\text{hered}$ --- Heritability: intergenerational genome hash
  stability, approximating $h^2$ from default mutation parameters.
\item $S_\text{select}$ --- Differential success via the Price equation
  \citep{price_1970_selection}: $\Delta\bar{z} = \text{Cov}(w,z)/\bar{w}$,
  where $w$ is per-parent offspring count (fitness) and $z$ is mean
  parent--child genome L2 distance, measuring selection on \emph{change
  magnitude} (how much the genome varies between generations) rather than
  direct ecological adaptation.  We use the \emph{selection} component only
  (transmission term fixed to zero), replacing the earlier
  correlation-based proxy.
\end{itemize}

The aggregate is the geometric mean of all three, ensuring that a family
with zero reproduction (F3) scores zero regardless of heritability or
selection signals.

\subsection{D3: Autonomy / Organizational Closure}

D3 operationalizes autopoiesis \citep{varela_1974_autopoiesis} as a
graph property.  Five process variables (energy, waste, boundary,
birth count, maturity) form nodes.  Edge detection proceeds in four
ordered steps:
\begin{enumerate}
\item \emph{Estimate lag}: Granger causality uses OLS lag regressions
  \citep{granger_1969_causal} at lags 1--5, selecting the lag with
  the lowest Holm--Bonferroni-corrected $F$-test $p$-value.
\item \emph{Compute TE and Granger per pair}: transfer entropy estimated
  with 5 \emph{quantile} bins at lag~1, significance by 400 permutation
  tests over shuffled source lags.
\item \emph{Per-pair Bonferroni}: combine the two tests via
  $p_\text{pair} = \min(1,\; 2 \cdot \min(p_\text{TE}, p_\text{Granger}))$.
\item \emph{BH-FDR}: apply Benjamini--Hochberg FDR control at $q = 0.05$
  \citep{benjamini_1995_controlling} across all $5 \times 4 = 20$ pairs.
\end{enumerate}
The 200-sample time series ($n = 200$ at 10-step intervals over
2{,}000 steps) provides adequate statistical power for both estimators;
robustness to bin count ($\{5, 10, 20\}$) and lag ($\{1, 2, 3\}$) is
verified in exploratory robustness checks (not shown for space).

The \emph{closure score} is the fraction of process variables in the
largest strongly connected component (SCC) of the influence graph
(Tarjan's algorithm; singleton SCCs excluded).
To avoid circularity with the alive-count AUC prediction target,
the primary D3 score uses closure alone (\texttt{closure\_only} mode).
A secondary composite, closure $\times$ persistence, is reported
separately; it measures self-maintenance duration (fraction of steps
where population exceeds 50\% of initial size) but is not used in
predictive validity evaluation.
A non-reproducing family that persists through self-maintenance (F3)
can score well on D3 under either mode.
Sensitivity to the FDR $q$-value is assessed at $q \in \{0.01, 0.05, 0.10\}$
(\S\ref{sec:results}).

\subsection{D4: Information Maintenance}

D4 frames life as a system where heritable information causally maintains
itself \citep{schrodinger_1944_what, walker_2013_algorithmic}.
Three sub-scores:

\begin{itemize}
\item $S_\text{present}$ --- Non-trivial heritable information exists:
  genome drift $> 0$ and diversity compared to cross-family baseline.
\item $S_\text{causal}$ --- Information predicts fitness: Spearman
  $\rho$ between genome diversity and alive count, plus transfer
  entropy from diversity to alive count (bonus for significance).
\item $S_\text{preserved}$ --- Information persists across generations:
  Jaccard similarity of 64-bit FNV-1a genome hashes between consecutive
  generations (collision probability ${\approx}\,2.7 \times 10^{-12}$
  for $n \approx 10^4$ genomes).
\end{itemize}

The aggregate is a weighted average with double weight on the causal
component: $(S_\text{present} + 2 \cdot S_\text{causal} +
S_\text{preserved}) / 4$.  The $2\times$ causal weight reflects
D4's core thesis that information must \emph{do causal work}---mere
presence or persistence is necessary but not sufficient.

% ============================================================================
\section{Experiments}
\label{sec:experiments}

\paragraph{Environment regimes.}
Five regimes stress-test different aspects of organism viability.
We state a priori predictions for each:
\textbf{E1} (baseline)---all families viable, D1--D4 agree on F1;
\textbf{E2} (resource scarcity: halved regeneration, larger world)---metabolic
stress expected to reduce D3 closure for all families;
\textbf{E3} (crowding: 80 organisms, 30 agents, smaller world)---competition
pressure expected to amplify D2/D3 divergence;
\textbf{E4} (sensing noise: Gaussian $\sigma=0.5$ on neural inputs)---should
degrade D1's $\gamma$ condition (coupling via TE) more than D2's
reproductive statistics;
\textbf{E5} (spatial patchiness: 4 resource patches, $2\times$ peak
regeneration)---spatial heterogeneity may advantage organisms with
better stimulus response.

\paragraph{Data separation.}
Following a pre-specified evaluation split, we split seeds into
\emph{calibration} (seeds 0--99) and \emph{held-out test} (seeds 100--199).
All threshold tuning and adapter development use only calibration seeds.
Test seeds are evaluated with frozen thresholds; ``unseen conditions''
refers to unseen seeds within the same regimes, not unseen regimes.

\paragraph{Single-family controls.}
To disentangle competition from capability deficits, each family type is
also run alone (Mode~B with a single family entry) across all five
regimes and 200 seeds.  This preserves the full scoring pipeline
(\texttt{family\_breakdown} output is identical) while eliminating
inter-family resource competition.

\paragraph{Scoring.}
Each (regime $\times$ seed $\times$ family) triple is scored by all four
adapters, producing a score matrix of $5 \times 100 \times 3 \times 4 =
6{,}000$ graded scores (calibration set).
The default pass/fail threshold is 0.3, chosen as a round value below the
empirical median score of the positive control (F1) to separate viable
from non-viable families; calibrated per-definition thresholds are reported
in Table~\ref{tab:thresholds}.

\begin{table}[t]
\centering
\small
\caption{Calibrated thresholds (maximizing balanced accuracy on seeds 0--99)
vs.\ the default 0.3.}
\label{tab:thresholds}
\begin{tabular}{@{}lcc@{}}
\toprule
Definition & Default & Calibrated \\
\midrule
D1 & 0.30 & 0.39 \\
D2 & 0.30 & 0.29 \\
D3 & 0.30 & 0.61 \\
D4 & 0.30 & 0.58 \\
\bottomrule
\end{tabular}
\end{table}

Strict-mode thresholds, calibrated separately on the same seed split: D1\,=\,0.33, D2\,=\,0.29, D3\,=\,0.61, D4\,=\,0.25.

\paragraph{Agreement metrics.}
Pairwise agreement: Cohen's $\kappa$ (binary), Spearman $\rho$ (graded),
percent agreement.  Aggregate: Fleiss' $\kappa$ for 4-rater agreement.
Block bootstrap CIs (2,000 resamples at the seed level) preserve
within-seed correlation.  For ROC-AUC CIs, binary labels are fixed
from the full test-set median split so that the bootstrap estimand
matches the reported point estimate.

\paragraph{Multiple comparison corrections.}
D3 edge detection applies per-pair Bonferroni followed by
Benjamini--Hochberg FDR across all 20 directed pairs---this controls the
false discovery rate at $q = 0.05$.  Bootstrap CIs for agreement metrics
are \emph{descriptive} (no multiple-comparison correction), as they
characterize estimator uncertainty rather than test hypotheses.  Pairwise
AUC difference CIs (6 pairs) are reported with Bonferroni-adjusted
99.2\% intervals (adjusted $\alpha = 0.05/6 = 0.0083$, $z \approx 2.638$);
significance is declared only when the adjusted interval excludes zero.
Under independence, the family-wise error rate at raw 95\% would reach
${\approx}26\%$ ($1 - 0.95^6$); the adjusted CIs formally control this.

\paragraph{Predictive validity.}
For each definition, we sweep thresholds on the calibration set to
maximize balanced accuracy of predicting whether the alive-count AUC
over the last 30\% of the run exceeds the calibration-set median.
Optimal thresholds are frozen, then evaluated on test seeds via
ROC-AUC, precision, recall, and a $\pm 20\%$ sensitivity analysis.
To reduce target-coupling leakage in alive-count prediction, we also report
a strict evaluation mode where D1 zeroes alive-count-targeted coupling
terms ($\gamma$ scores for Growth, Reproduction, and Evolution) and D4
replaces \texttt{alive\_count} with \texttt{energy\_mean} in
$S_\text{causal}$; D2 and D3 are unchanged.
To test robustness against substrate bias, we additionally evaluate
two alternative prediction targets: \emph{recovery time} (normalized
steps to recover population after the deepest dip) and
\emph{lineage diversity} (ratio of unique reproducing parent genotypes
to total birth events in the final 20\% of the lineage).

\paragraph{Sensitivity analysis.}
Three sweeps assess robustness of adapter parameterization:
(1)~D1 weight perturbation ($\pm 20\%$ on $w_\alpha, w_\beta, w_\gamma$),
(2)~binary threshold sweep (0.1--0.9 for all definitions), and
(3)~D3 FDR $q$-value sweep ($q \in \{0.01, 0.05, 0.10\}$).

% ============================================================================
\section{Results}
\label{sec:results}

We summarize the main findings before presenting details.
Definitions disagree substantially: Fleiss' $\kappa = $ \fleissK{}
indicates only moderate 4-rater agreement.
The D2--D3 axis is the most informative disagreement, cleanly
separating evolution-centric from organization-centric perspectives.
Under strict (leakage-aware) evaluation, D1 achieves the highest
predictive validity for population robustness (AUC = \aucDstrict{D1},
95\% CI: \aucDciStrict{D1}), while D4 drops substantially
(AUC = \aucDstrict{D4}, 95\% CI: \aucDciStrict{D4}), revealing that
D4's causal component captures population-level rather than
organism-level information maintenance; D2 and D3 are unaffected
by strict mode (\aucDstrict{D2}, \aucDstrict{D3}).
Under legacy evaluation the ranking compresses under alternative targets,
confirming complementary information capture.
These patterns are robust to operationalization variants, $\pm 20\%$
weight perturbation, and removal of inter-family competition.

\paragraph{Disagreement heatmap.}
Figure~\ref{fig:heatmap} shows mean D1--D4 scores across regime--family
combinations.  The expected diagonal pattern emerges: F1 scores highly
on all definitions, F2 scores highly on D2 but lower on D3, and F3
scores near zero on D2 but retains moderate D3 scores.  D1 is
uniformly strict for ablated families due to its geometric-mean
aggregate.

\begin{figure}[t]
\centering
\includefigure{figures/disagreement_heatmap.pdf}
\caption{Disagreement heatmap.  Each cell shows the mean definition score
across seeds.  Rows: regime $\times$ family; columns: D1--D4.}
\label{fig:heatmap}
\end{figure}

\paragraph{Agreement matrix.}
Figure~\ref{fig:agreement} presents the $4 \times 4$ agreement matrix.
Binary agreement is highest for D1--D2
($\kappa = $ \kappaDD{D1D2}, 95\% CI: \kappaDDci{D1D2}),
reflecting shared reliance on reproduction, and lowest for D3--D4
($\kappa = $ \kappaDD{D3D4}, 95\% CI: \kappaDDci{D3D4}).
Graded agreement is highest for D1--D3
($\rho = $ \rhoDD{D1D3}, 95\% CI: \rhoDDci{D1D3}),
showing that definitions can rank families similarly while disagreeing on
which cross the pass/fail boundary.
The aggregate Fleiss' $\kappa$ is \fleissK{} (95\% CI: \fleissKci{}).

\begin{figure}[t]
\centering
\includefigure[width=0.85\columnwidth]{figures/agreement_matrix.pdf}
\caption{Agreement matrix.  Lower triangle: Cohen's $\kappa$ (binary);
upper triangle: Spearman $\rho$ (graded).}
\label{fig:agreement}
\end{figure}

\paragraph{Case studies.}
Figure~\ref{fig:casestudy} illustrates two diagnostic disagreements.
\emph{Left panel}: F3 (autonomy) maintains stable energy and boundary
signals throughout the run, earning a moderate D3 closure score,
while D2 assigns zero due to absent reproduction.
\emph{Right panel}: F2 (Darwinian) shows sustained births and genome
diversity, earning high D2 scores, but its broken boundary--energy
feedback loop reduces D3 closure.

\begin{figure*}[t]
\centering
\includefigure[width=\textwidth]{figures/case_study_timeseries.pdf}
\caption{Case study time series.  Left: F3 (D3 $>$ D2).
Right: F2 (D2 $>$ D3).}
\label{fig:casestudy}
\end{figure*}

\paragraph{D3 closure and Price selection.}
Under \texttt{closure\_only} mode, D3 scores reflect purely organizational
closure; the D2--D3 disagreement axis is preserved (F3 achieves non-zero
closure while scoring zero on D2).
The Price-based $S_\text{select}$ sub-score yields positive selection
coefficients for F1 (0.30) and F2 (0.48), strengthening D2's
discrimination via a direct lineage-derived signal that avoids
proxy-based correlation measures.

\paragraph{Competition controls.}
Single-family runs (3 families $\times$ 5 regimes $\times$ 200 seeds)
confirm the F1 $>$ F2 $>$ F3 ordering on D1 in isolation
(E1: 0.88 $>$ 0.80 $>$ 0.47).
F3's D3 closure rises without competition
(0.37 vs.\ 0.21 in E1, $\Delta = +0.16$); F2's D1 also rises
(0.80 vs.\ 0.34), showing competition amplifies apparent capability
differences.  The D2--D3 disagreement axis is preserved: F3 still
receives D2 = 0 but D3 $>$ 0 in both conditions.

\paragraph{Predictive validity (confirmatory).}
Figures~\ref{fig:roc_legacy} and~\ref{fig:roc_strict} show ROC performance
for strict and legacy predictive evaluation on the held-out test set
(Table~\ref{tab:predictive}).
Under strict predictive evaluation (leakage-aware coupling suppression),
D1 achieves the highest validity (AUC = \aucDstrict{D1},
95\% CI: \aucDciStrict{D1}), while D2 and D3 are unaffected
(\aucDstrict{D2}, \aucDstrict{D3}).
D4 drops substantially to \aucDstrict{D4} (95\% CI: \aucDciStrict{D4}),
indicating that its predictive strength is strongly tied to
alive-count-linked causal fitness signals---a finding elaborated in
\S\ref{sec:discussion}.
Under legacy evaluation, D4 rises to \aucD{D4} (95\% CI: \aucDci{D4}),
and the full ordering is D1 (\aucD{D1}) $>$ D4 (\aucD{D4}) $>$ D3
(\aucD{D3}) $>$ D2 (\aucD{D2}).
D2 shows the lowest AUC in both modes because its three sub-scores
(reproduction, heredity, selection) produce a narrower score range than
D1's seven criteria, limiting threshold discrimination.

\begin{table}[t]
\centering
\small
\caption{Predictive validity on test seeds (100--199), side-by-side legacy vs strict.
Primary target: alive-count AUC.}
\label{tab:predictive}
\begin{tabular}{@{}lcccc@{}}
\toprule
 & D1 & D2 & D3 & D4 \\
\midrule
ROC-AUC (legacy)  & \aucD{D1} & \aucD{D2} & \aucD{D3} & \aucD{D4} \\
ROC-AUC (strict)  & \aucDstrict{D1} & \aucDstrict{D2} & \aucDstrict{D3} & \aucDstrict{D4} \\
% Hardcoded from experiments/benchmark/predictive_analysis.json,
% predictive_analysis_strict.json, and robustness_table.json.
% Regenerate via scripts/analyze_predictive.py — update if pipeline is re-run.
Balanced Acc.\ (legacy) & 0.85 & 0.66 & 0.74 & 0.78 \\
Balanced Acc.\ (strict) & 0.86 & 0.66 & 0.74 & 0.55 \\
Precision (legacy) & 0.99 & 0.63 & 0.72 & 1.00 \\
Precision (strict) & 0.99 & 0.63 & 0.72 & 0.54 \\
Recall (legacy)    & 0.70 & 0.78 & 0.80 & 0.55 \\
Recall (strict)    & 0.72 & 0.78 & 0.80 & 0.74 \\
\bottomrule
\end{tabular}
\end{table}

Under alternative targets (recovery time, lineage diversity), the ranking
compresses---all definitions achieve AUC 0.77--0.92---indicating
complementary information capture.
Leave-one-regime-out cross-validation confirms the D1 $>$ D4 $>$ D3 $>$
D2 ordering is not artefactual to any single regime.

\begin{figure}[t]
\centering
\includefigure[width=0.85\columnwidth]{figures/predictive_roc.pdf}
\caption{Legacy-mode ROC curves on held-out test seeds (100--199).}
\label{fig:roc_legacy}
\end{figure}

\begin{figure}[t]
\centering
\includefigure[width=0.85\columnwidth]{figures/predictive_roc_strict.pdf}
\caption{Strict-mode ROC curves on held-out test seeds (100--199).}
\label{fig:roc_strict}
\end{figure}

\paragraph{Sensitivity analysis (exploratory).}
D1 weight perturbation ($\pm 20\%$) changes mean scores by $\leq 0.019$,
preserving family ordering (rank correlations with default: 0.75--1.00).
Binary threshold sweep (0.1--0.9) yields monotonically decreasing pass rates.
D3 FDR sweep ($q \in \{0.01, 0.05, 0.10\}$) shifts mean closure from 0.48
to 0.61 (range $\Delta = 0.13$) but preserves the D2--D3 disagreement axis
(F3 remains D2$=0$, D3$>0$ at all $q$).  D4 similarity mode (L2 vs.\ hash)
shows rank correlation $\rho = 0.94$, confirming family ordering stability.
Table~\ref{tab:robustness} summarizes rank stability across all
operationalization variants.
Phase-randomized surrogate analysis (100 surrogates per pair, 50 seeds,
E1) confirms TE-based coupling detection is not systematically producing
false positives (mean FPR = 0.078 $<$ 0.10).

\begin{table}[t]
\centering
\small
\caption{Rank stability of key findings under alternative operationalizations.
``D2--D3 axis'' = F3 scores D3$>$D2 and F2 scores D2$>$D3.
``Family ordering'' = F1$>$F2$>$F3 rank preserved.
$\rho$ = Spearman rank correlation with default variant.}
\label{tab:robustness}
\begin{tabular}{@{}llcc@{}}
\toprule
Adapter & Variant & Axis & Ordering ($\rho$) \\
\midrule
D1 & harmonic mean & \checkmark & 1.00 \\
D1 & min aggregation & \checkmark & 0.97 \\
D1 & arithmetic mean & \checkmark & 0.75 \\
D3 & FDR $q=0.01$ & \checkmark & --- \\
D3 & FDR $q=0.10$ & \checkmark & --- \\
D3 & AND-condition edges & $\times^*$ & 0.17$^*$ \\
D4 & L2 similarity & \checkmark & 0.94 \\
\bottomrule
\multicolumn{4}{@{}p{0.85\columnwidth}@{}}{\footnotesize $^*$AND-condition collapses
  most edges (mean closure 0.027 vs.\ 0.579 for Bonferroni); primary mode is Bonferroni.}
\end{tabular}
\end{table}

% ============================================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{What the disagreements reveal.}
The D2--D3 disagreement axis is the most informative: it cleanly separates
evolution-centric from organization-centric perspectives.  F3 satisfies D3
but not D2; F2 satisfies D2 but scores lower on D3.  This is not a flaw
in either definition but a genuine ontological divergence about which
processes are essential---echoing the ``basic autonomy + open-ended
evolution'' synthesis of \citet{ruizmirazo_2004_universal}.
D1, by requiring \emph{all} seven criteria, subsumes both perspectives
but is too strict to distinguish borderline cases.

\paragraph{Regime and competition effects.}
Under E2 (resource scarcity), even F1 shows reduced D3 closure as stress
disrupts feedback coupling; under E4 (sensing noise), D1 drops more
sharply than D2 because noisy inputs degrade TE coupling ($\gamma$)
while discrete reproductive statistics remain robust.
Single-family controls confirm the disagreement structure persists
without competition, though F3's D3 closure improves modestly in isolation.
D1's $\beta$ condition compares families against ablated counterparts,
so scores depend on co-existing family composition; in single-family
runs, $\beta$ defaults to zero and D1 relies on $\alpha$ and $\gamma$ alone.

\paragraph{Predictive validity trade-offs.}
D1's seven criteria capture the broadest signal across evaluation modes
within our substrate and prediction targets (legacy AUC = \aucD{D1},
strict AUC = \aucDstrict{D1}).
D2's narrower score range limits threshold discrimination.
Under lineage diversity all definitions converge (AUC 0.77--0.83),
consistent with each definition measuring a distinct facet of viability;
predictive validity is substrate- and target-dependent, not a ``correctness''
ranking.

\paragraph{D4 strict-mode drop: leakage or core mechanism?}
D4 shows the largest mode sensitivity (legacy \aucD{D4} to strict
\aucDstrict{D4}), admitting two readings.
The \emph{leakage} view (adopted here): $S_\text{causal}$ measures
genome-to-alive-count coupling, partially tautological when predicting
alive-count AUC.
The \emph{mechanistic} view: alive-count-linked causality is D4's core
mechanism---information maintaining itself via population persistence.
The drop reveals that D4's causal component currently captures
population-level rather than organism-level information maintenance;
redesigning $S_\text{causal}$ to be alive-count-independent is left for
future work.

\paragraph{Limitations.}
Different operationalizations could shift agreement patterns, though
$\pm 20\%$ weight perturbation and alternative aggregation, edge, and
similarity modes preserve rank stability (Table~\ref{tab:threats}).
Explicit reproduction and continuous 256-float genomes may favor
evolution-centric definitions; alternative targets and Lenia
(\S\ref{sec:lenia}) partially address this, but fundamentally different
substrates (chemical networks, protocells) are needed to establish
generality---making substrate bias the most consequential residual threat
(Table~\ref{tab:threats}).
For the Price equation, only the selection component is reported
(transmission fixed to zero); the full decomposition is left for
future work.
D3's SCC membership shows moderate Jaccard stability across consecutive
time windows (mean $J = 0.57$, SD $= 0.39$, $n = 1{,}500$
comparisons); longer runs may be needed to characterize steady-state
closure.

\begin{table}[t]
\centering
\small
\caption{Threats to validity and their mitigations.}
\label{tab:threats}
\begin{tabular}{@{}p{2.5cm}p{4.9cm}@{}}
\toprule
Threat & Mitigation \\
\midrule
Substrate bias (evolution-centric design) &
  Alternative prediction targets (recovery time, lineage diversity);
  Lenia cross-substrate test (\S\ref{sec:lenia}) \\
Operationalization dependence &
  Robustness sweeps over D1 aggregation, D3 edge mode,
  D4 similarity mode; rank stability across variants \\
TE/Granger estimation noise &
  Surrogate FPR (all regimes); estimator parameter sweep
  (bins, lag); closure\textunderscore only mode \\
Competition confounds &
  Single-family control experiments across all regimes \\
D3 circularity with alive-count &
  closure\textunderscore only mode as primary D3 score \\
D1 cross-family $\beta$ dependence &
  $\beta$ rank stability analysis; single-family fallback \\
Multiple comparisons &
  Explicit correction hierarchy; confirmatory/exploratory
  separation (\S\ref{sec:experiments}) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Future work.}
Open directions include: the full Price selection--transmission
decomposition; self-replicating CA substrates (e.g., Flow-Lenia) for a
non-trivial D2--D3 cross-substrate test; Lenia-to-JSON mapping sensitivity
analysis; and a D4 $S_\text{causal}$ redesign that is alive-count-independent.
Adding a fifth definition requires only implementing a single
\texttt{score(run\_summary, family\_id)} function; the benchmark harness
handles all data loading, scoring, and analysis.

% ============================================================================
\subsection{Cross-Substrate Validation: Lenia}
\label{sec:lenia}

To test whether the D2--D3 disagreement axis generalizes beyond our
evolution-centric substrate, we apply the D1--D4 adapters to Lenia
\citep{chan_2019_lenia}, a continuous cellular automaton where
spatiotemporal patterns self-organize without explicit reproduction.
We state a pre-specified hypothesis
(\textbf{H\textsubscript{Lenia}}): \emph{in a substrate without explicit
reproduction, D3 will score higher than D2.}
We implement a minimal Lenia simulator using the standard update rule
$A(t+1) = \text{clip}(A(t) + \Delta t \cdot G(K \ast A(t)),\, 0,\, 1)$,
where $K$ is a ring-shaped kernel and $G$ is a Gaussian growth function.
Three known stable creatures (Orbium, Geminium, Scutium) are simulated
at $128 \times 128$ resolution for 500 steps with 5 seeds each.

A priori mappings (mass $\to$ \texttt{energy\_mean}, compactness $\to$
\texttt{boundary\_mean}, pattern entropy $\to$ \texttt{waste\_mean},
connected components $\to$ \texttt{alive\_count}, birth count $= 0$)
let \texttt{score\_all()} run without adapter changes, testing framework
generalization.
Across all creatures and seeds, D3 consistently scores higher than D2
(Orbium: $\bar{D3} = 0.80$; Geminium: 0.60; Scutium: 0.80; all
$\bar{D2} = 0.00$), consistent with H\textsubscript{Lenia}.
The non-trivial result is D3$> 0$ on a non-reproductive substrate,
supporting the D2--D3 axis as a genuine conceptual divide.
Flow-Lenia \citep{plantec_2023_flow}, which supports self-replication,
would provide a stronger test.

% ============================================================================
\section{Artifact Release}
\label{sec:artifact}

Code, adapters, and benchmark data are released under
MIT license (code) and CC-BY~4.0 (data) at
\url{https://github.com/TheIllusionOfLife/alife_definitions}.
Code is archived at \url{https://doi.org/10.5281/zenodo.19351498};
raw benchmark data at \url{https://doi.org/10.5281/zenodo.19397019}.
The full pipeline regenerates all results from raw runs (${\sim}$30\,h
on a Mac Mini M2~Pro; 8 workers); core numerical results are fully
reproducible from the adapter logic (\S\ref{sec:adapters}) and
experimental design (\S\ref{sec:experiments}) alone.
Robustness evidence is detailed in \S\ref{sec:results}--\S\ref{sec:lenia}
and Tables~\ref{tab:robustness}--\ref{tab:threats}.

% ============================================================================
\section*{Acknowledgments}
This research received no external funding.

\footnotesize
\bibliographystyle{apalike}
\bibliography{references}

\end{document}
